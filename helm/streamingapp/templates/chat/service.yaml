apiVersion: v1
kind: Service
metadata:
  name: chat
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "streamingapp.labels" . | nindent 4 }}
    app.kubernetes.io/name: chat
spec:
  type: ClusterIP
  selector:
    {{- include "streamingapp.selectorLabels" (dict "component" "chat" "Release" .Release) | nindent 4 }}
  ports:
    - port: {{ .Values.chat.port }}
      targetPort: {{ .Values.chat.port }}
      protocol: TCP
      name: http
